{
  "name": "react-native-box-cli",
  "version": "0.0.2",
  "description": "",
  "main": "src/react/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/babel src/node --out-dir src/node/.build --ignore .build",
    "build-watch": "./node_modules/.bin/babel src/node --out-dir src/node/.build -w --ignore .build"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cli-color": "^1.2.0",
    "commander": "^2.11.0",
    "react": "^15.6.1"
  },
  "bin": {
    "box": "src/node/dist/cli.js"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react": "^6.24.1",
    "babel-preset-react-native": "^3.0.2"
  }
}
